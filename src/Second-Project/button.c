#include <SDL2/SDL.h>
// #include <SDL2/SDL_ttf.h>

// void LoadFont(SDL_Renderer *renderer) {
	// SDL_Color black = {0, 0, 0};
	// TTF_Font *font = TTF_OpenFont("Assets/Helvetica.ttf");
	// SDL_Surface* surface = TTF_RenderText_Solid(font, "Test", black);
	// SDL_Texture* Message = SDL_CreateTextureFromSurface(renderer, surface);
	// SDL_Rect btn = { .x = 0, .y = 0, .w = 100, .h = 300 };
	// SDL_RenderCopy(renderer, Message, NULL, &btn);
	// SDL_FreeSurface(surface);
	// SDL_DestroyTexture(Message);
// }
